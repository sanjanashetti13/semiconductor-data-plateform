# Lean dependency set for Vercel serverless (no native ODBC / pyodbc).
# Azure SQL requires pyodbc + drivers — use Azure App Service / Render / Railway
# or run uvicorn locally for full database features.
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
python-dotenv>=1.0.0
groq>=0.9.0
