# =============================================================================
# Production dependencies — Azure App Service (FastAPI + React static)
# Install: pip install -r requirements.txt
# For local ETL / Spark / notebooks see requirements-etl.txt
# =============================================================================

fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
python-dotenv>=1.0.0
groq>=0.9.0
pyodbc>=5.0.0
